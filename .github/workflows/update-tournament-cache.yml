# Každý měsíc obnoví turnajový plán z ITF Juniors: první běh stáhne 4 měsíce, další běhy přidají jeden měsíc.
# Do DB se importuje vždy jen okno „od teď + 4 měsíce“ jako čistý upsert (bez mazání existujících řádků).
# Spouští se 1. v měsíci ve 4:00 UTC (4:00 středoevropského času v zimě, 6:00 v létě).

name: Update tournament cache (4 months)

on:
  schedule:
    - cron: '0 4 1 * *'
  workflow_dispatch:

jobs:
  update-cache:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Chrome for Puppeteer
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends wget ca-certificates
          sudo mkdir -p /etc/apt/keyrings
          wget -q -O - https://dl.google.com/linux/linux_signing_key.pub | sudo tee /etc/apt/keyrings/google.asc > /dev/null
          echo "deb [arch=amd64 signed-by=/etc/apt/keyrings/google.asc] http://dl.google.com/linux/chrome/deb/ stable main" | sudo tee /etc/apt/sources.list.d/google-chrome.list
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable
          echo "PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable" >> $GITHUB_ENV

      - name: Restore tournament cache state
        uses: actions/cache/restore@v4
        with:
          path: data
          key: tournament-cache-state

      - name: Prepare data dir
        run: mkdir -p data

      - name: Decide mode and set next month
        id: mode
        run: |
          if [ -f data/tournament-cache-state.json ]; then
            echo "mode=incremental" >> $GITHUB_OUTPUT
            NEXT=$(node scripts/get-next-calendar-month.js data/tournament-cache-state.json | sed 's/^NEXT_MONTH=//')
            echo "next_month=$NEXT" >> $GITHUB_OUTPUT
          else
            echo "mode=full" >> $GITHUB_OUTPUT
            echo "next_month=" >> $GITHUB_OUTPUT
          fi

      - name: Full run – set 4 months (M1–M4)
        if: steps.mode.outputs.mode == 'full'
        id: months
        run: |
          node -e "
          const d = new Date();
          const m1 = d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0');
          d.setMonth(d.getMonth() + 1);
          const m2 = d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0');
          d.setMonth(d.getMonth() + 1);
          const m3 = d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0');
          d.setMonth(d.getMonth() + 1);
          const m4 = d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0');
          console.log('M1=' + m1);
          console.log('M2=' + m2);
          console.log('M3=' + m3);
          console.log('M4=' + m4);
          " >> $GITHUB_ENV

      - name: Full run – fetch month 1
        if: steps.mode.outputs.mode == 'full'
        run: node scripts/fetch-calendar-itf-juniors.js "$M1"

      - name: Full run – merge baseline
        if: steps.mode.outputs.mode == 'full'
        run: cp data/tournament-cache.json data/all.json

      - name: Full run – fetch month 2
        if: steps.mode.outputs.mode == 'full'
        run: node scripts/fetch-calendar-itf-juniors.js "$M2"

      - name: Full run – merge month 2
        if: steps.mode.outputs.mode == 'full'
        run: node scripts/merge-tournament-cache.js data/all.json data/tournament-cache.json data/all.json

      - name: Full run – fetch month 3
        if: steps.mode.outputs.mode == 'full'
        run: node scripts/fetch-calendar-itf-juniors.js "$M3"

      - name: Full run – merge month 3
        if: steps.mode.outputs.mode == 'full'
        run: node scripts/merge-tournament-cache.js data/all.json data/tournament-cache.json data/all.json

      - name: Full run – fetch month 4
        if: steps.mode.outputs.mode == 'full'
        run: node scripts/fetch-calendar-itf-juniors.js "$M4"

      - name: Full run – merge month 4 and finalize
        if: steps.mode.outputs.mode == 'full'
        run: |
          node scripts/merge-tournament-cache.js data/all.json data/tournament-cache.json data/all.json
          cp data/all.json data/tournament-cache.json
          cp data/all.json data/tournament-cache-state.json

      - name: Incremental run – fetch next month only
        if: steps.mode.outputs.mode == 'incremental'
        run: node scripts/fetch-calendar-itf-juniors.js "${{ steps.mode.outputs.next_month }}"

      - name: Incremental run – merge with state and finalize
        if: steps.mode.outputs.mode == 'incremental'
        run: |
          node scripts/merge-tournament-cache.js data/tournament-cache-state.json data/tournament-cache.json data/all.json
          cp data/all.json data/tournament-cache.json
          cp data/all.json data/tournament-cache-state.json

      - name: Validate cache before import
        run: |
          node scripts/validate-tournament-cache.js data/tournament-cache.json

      - name: Import to Supabase (okno 4 měsíců dopředu, upsert-only, bez mazání)
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: node scripts/import-tournament-cache.js data/tournament-cache.json --from-today --window-months=4

      - name: Save tournament cache state to cache
        uses: actions/cache/save@v4
        with:
          path: data
          key: tournament-cache-state
